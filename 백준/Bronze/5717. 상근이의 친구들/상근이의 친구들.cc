#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(nullptr);

	int m, f;
	while(cin >> m >> f) {
		if (m == 0 && f == 0) break;
		cout << m + f << '\n';
	}

}